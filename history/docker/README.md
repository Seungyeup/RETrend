# Docker

컨테이너 이미지 빌드 정의(Dockerfile) 레이어입니다.

현재 Dockerfile 위치:
- Crawler image: `docker/crawler/Dockerfile`
- KREB backfill image: `docker/kreb-backfill/Dockerfile`
- Spark image: `docker/spark/spark-py-s3a.Dockerfile`
